[build]
builder = "nixpacks"

[deploy]
startCommand = "uv run uvicorn todo_api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
